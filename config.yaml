db:
    mongodb:
      host: localhost
      port: 27017
      username: beepaste
      password: my secret password
      database: beepaste
    redis:
      host: localhost
      port: 6379
      password: my secret password 
api:
    use_anonymous: yes

web_server:
    port : 8080
    host : localhost
    workers: 8
    log_config: None
